var __defProp = Object.defineProperty;
var __markAsModule = (target) => __defProp(target, "__esModule", { value: true });
var __export = (target, all) => {
  __markAsModule(target);
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};

// src/abstraction/index.ts
__export(exports, {
  default: () => abstraction_default
});
var fs = {
  async readdir(path) {
    const res = await fetch(`http://localhost:7070/${path}?command=readdir`);
    if (res.status === 200) {
      const data = await res.json();
      return data;
    }
    throw new Error(await res.text());
  },
  async readFile(path) {
    const res = await fetch(`http://localhost:7070/${path}?command=readFile`);
    if (res.status === 200) {
      const data = await res.text();
      return data;
    }
    throw new Error(await res.text());
  },
  async stat(path) {
    const res = await fetch(`http://localhost:7070/${path}?command=stat`);
    if (res.status === 200) {
      const data = await res.json();
      return data;
    }
    throw new Error(await res.text());
  },
  async writeFile(path, data) {
    await fetch(`http://localhost:7070/${path}`, {
      method: "POST",
      headers: {
        "Content-Type": "application/json"
      },
      body: JSON.stringify({ data })
    });
  },
  async rm(path, options) {
    const res = await fetch(`http://localhost:7070/${path}${(options == null ? void 0 : options.recursive) ? "?recursive=true" : ""}`, {
      method: "DELETE"
    });
    if (res.status === 200) {
      return;
    }
    throw new Error(await res.text());
  }
};
var abstraction_default = fs;
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {});
